export const NEWS_WIDGET_CLIENT = String.raw`(()=>{"use strict";
const MAX_ARTICLES=100,MAX_ACTIONS=50,root=document.getElementById("app");let actions=[];
const allowed=new Set(["P","BR","H2","H3","STRONG","EM","UL","OL","LI","BLOCKQUOTE","A"]),object=value=>value!==null&&typeof value==="object"&&!Array.isArray(value),node=(tag,value,cls)=>{const item=document.createElement(tag);item.textContent=String(value??"");if(cls)item.className=cls;return item};
const valid=model=>object(model)&&model.widget==="news"&&model.contract_version==="1.0.0"&&object(model.club)&&object(model.data)&&Array.isArray(model.data.articles)&&model.data.articles.length<=MAX_ARTICLES&&Array.isArray(model.actions)&&model.actions.length<=MAX_ACTIONS&&model.actions.every(action=>object(action)&&action.visibility==="visible");
const safeRich=html=>{if(typeof html!=="string"||html.length>200000)return null;const parsed=new DOMParser().parseFromString(html,"text/html"),fragment=document.createDocumentFragment();const copy=(source,target)=>{source.childNodes.forEach(child=>{if(child.nodeType===Node.TEXT_NODE){target.append(document.createTextNode(child.textContent||""));return}if(child.nodeType!==Node.ELEMENT_NODE||!allowed.has(child.tagName))return;const out=document.createElement(child.tagName.toLowerCase());if(child.tagName==="A"){const href=child.getAttribute("href");try{const url=new URL(href||"");if(url.protocol!=="https:"||url.username||url.password)return;out.href=url.toString();out.target="_blank";out.rel="noopener noreferrer"}catch{return}}copy(child,out);target.append(out)})};copy(parsed.body,fragment);return fragment};
const status=(title,message,retry=false)=>{root.replaceChildren();const page=node("main","","page"),panel=node("section","","widget state-panel");panel.setAttribute("role","status");panel.append(node("h2",title),node("p",message));if(retry){const button=node("button","Erneut versuchen","btn btn-secondary");button.type="button";button.addEventListener("click",initialize);panel.append(button)}page.append(panel);root.append(page)};
const call=(action,index)=>{if(!object(action)||action.enabled!==true||action.visibility!=="visible"||typeof action.tool_name!=="string"||!object(action.input))return;window.parent.postMessage({jsonrpc:"2.0",id:"tool-"+Date.now()+"-"+index,method:"tools/call",params:{name:action.tool_name,arguments:action.input}},"*")};
const newsId=action=>object(action)&&object(action.input)&&typeof action.input.news_id==="string"?action.input.news_id:null;
const render=model=>{if(!valid(model)){status("News nicht verfügbar","Die News-Daten konnten nicht sicher verarbeitet werden.",true);return}actions=model.actions.filter(action=>action.visibility==="visible");root.replaceChildren();const page=node("main","","page"),widget=node("section","","widget"),head=node("header","","widget-head"),heading=node("div","");heading.append(node("p","Comvenio · News","eyebrow"),node("h2",model.title),node("p","Öffentliche Beiträge lesen und berechtigte Entwürfe sicher verwalten."));const toolbar=node("div","","toolbar"),filter=node("label","Status","filter-label"),select=node("select","","field"),options=model.capability_version===null?[["public","Alle veröffentlichten"]]:[["all_authorized","Alle berechtigten"],["draft","Nur Entwürfe"]];select.setAttribute("aria-label","News-Status");options.forEach(([value,label])=>{const option=node("option",label);option.value=value;option.selected=value===model.data.filter;select.append(option)});filter.append(select);toolbar.append(filter);head.append(heading,toolbar);widget.append(head);const layout=node("div","","widget-body news-layout"),list=node("section","","stack news-list");list.setAttribute("aria-label","News-Beiträge");if(model.data.articles.length===0&&object(model.empty_state)){const empty=node("section","","state-panel");empty.append(node("h3",model.empty_state.title),node("p",model.empty_state.description));list.append(empty)}let selected=null;model.data.articles.forEach((article,index)=>{if(!object(article)||typeof article.news_id!=="string")return;if(article.news_id===model.data.selected_news_id)selected=article;const card=node("button","","news-card"+(article.news_id===model.data.selected_news_id?" selected":""));card.type="button";card.setAttribute("aria-pressed",String(article.news_id===model.data.selected_news_id));const label=article.status==="published"?"Veröffentlicht":article.status==="draft"?"Entwurf":"Archiviert";card.append(node("span",label,"status status-"+article.status),node("strong",article.title),node("span",article.published_at?new Date(article.published_at).toLocaleDateString("de-DE")+" · Öffentlich":"Noch nicht veröffentlicht · Nur Verwaltung","meta"));const actionIndex=actions.findIndex(action=>action.enabled===true&&action.risk_class==="read"&&newsId(action)===article.news_id);if(actionIndex>=0)card.addEventListener("click",()=>call(actions[actionIndex],actionIndex));else card.disabled=true;list.append(card)});const preview=node("article","","preview news-preview");preview.append(node("p","Homepage-Vorschau","eyebrow"));if(selected){if(typeof selected.hero_url==="string"){const image=node("img","");image.src=selected.hero_url;image.alt="";preview.append(image)}preview.append(node("h2",selected.title));const rich=safeRich(selected.sanitized_html);if(rich){const body=node("div","","rich-news");body.append(rich);preview.append(body)}else preview.append(node("p",selected.summary||"Für diesen Beitrag ist keine sichere Inhaltsvorschau verfügbar."));const meta=node("div","","meta");meta.append(node("span",selected.status==="published"?"Öffentliche Website":"Nicht öffentlich"),node("span","Keine internen IDs"));preview.append(meta)}else preview.append(node("h3","Beitrag auswählen"),node("p","Eine echte Vorschau wird erst nach Auswahl eines Beitrags geladen."));layout.append(list,preview);widget.append(layout);const bar=node("div","","actions news-actions");actions.forEach((action,index)=>{if(action.enabled!==true||action.risk_class==="read")return;const text=action.risk_class==="critical_write"?"Wirkung prüfen: "+action.label:action.label,button=node("button",text,action.risk_class==="critical_write"?"btn btn-primary confirmation-intent":"btn btn-secondary");button.type="button";button.addEventListener("click",()=>call(action,index));bar.append(button)});if(bar.childNodes.length)widget.append(bar);page.append(widget);root.append(page)};
const resultModel=params=>{if(!object(params))return null;const result=object(params.result)?params.result:params;return object(result.structuredContent)?result.structuredContent:object(result.structured_content)?result.structured_content:result.widget==="news"?result:null};window.addEventListener("message",event=>{if(event.source!==window.parent||!object(event.data)||event.data.jsonrpc!=="2.0")return;if(event.data.method==="ui/notifications/tool-result"){const model=resultModel(event.data.params);if(model)render(model)}else if(typeof event.data.id!=="undefined"&&object(event.data.result)){const model=resultModel(event.data.result);if(model)render(model)}});
function initialize(){status("News werden geladen","Comvenio lädt nur veröffentlichte oder für dich freigegebene Beiträge.");window.parent.postMessage({jsonrpc:"2.0",id:"ui-init-1",method:"ui/initialize",params:{appInfo:{name:"Comvenio News",version:"1.0.0"},appCapabilities:{tools:{listChanged:false}}}},"*")}initialize();})();`;
