{
  "name": "comvenio-news-video",
  "private": true,
  "description": "Remotion sub-project for `comvenio news video` (K7) — 3 fixed templates: slideshow, result, teaser. Renders LOCALLY only (never in a backend service).",
  "scripts": {
    "studio": "remotion studio",
    "render": "node render.mjs"
  },
  "dependencies": {
    "@remotion/bundler": "^4.0.0",
    "@remotion/cli": "^4.0.0",
    "@remotion/renderer": "^4.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "remotion": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.5.0"
  }
}
